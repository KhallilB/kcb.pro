name: detect-affected

on:
  workflow_call:
    inputs:
      base:
        description: Git ref to diff against
        required: false
        type: string
        default: origin/main
      target:
        description: Nx target to check (build, test, lint …)
        required: true
        type: string
      upload_artifact:
        description: Upload affected.json as artifact
        required: false
        type: boolean
        default: false
    outputs:
      apps:
        description: JSON array of affected projects
        value: ${{ jobs.detect.outputs.apps }}

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      apps: ${{ steps.set.outputs.apps }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Detect affected apps
        id: set
        run: |
          apps=$(npx nx show projects --affected --base="${{ inputs.base }}" --target="${{ inputs.target }}" --json)
          echo "$apps"
          echo "apps=$apps" >> "$GITHUB_OUTPUT"

          # Save to file only if artifact upload is requested
          if [ "${{ inputs.upload_artifact }}" = "true" ]; then
            echo "$apps" > affected.json
          fi

      - name: Upload affected.json
        if: ${{ inputs.upload_artifact }}
        uses: actions/upload-artifact@v4
        with:
          name: affected-apps
          path: affected.json
